# Hoist all packages to root node_modules.
# Required for Metro (Expo) to resolve packages correctly in a monorepo.
# Without this, Metro cannot find packages that pnpm symlinks through its virtual store.
shamefully-hoist=true

# Suppress peer dependency warnings from the React Native ecosystem.
strict-peer-dependencies=false

# Ensure lockfile is always created/updated.
lockfile=true
