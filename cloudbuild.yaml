steps:
  - name: node:20
    entrypoint: bash
    args:
      - -c
      - |
        npm install -g pnpm firebase-tools
        pnpm install --frozen-lockfile
        firebase deploy --only functions,hosting --project voices-9a030 --non-interactive

options:
  logging: CLOUD_LOGGING_ONLY
